export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>NextAuth Shadow OAuth Gateway</h1>
      <p>This service handles OAuth and posts credentials to the vault callback.</p>
    </main>
  );
}
